#include <stdio.h>
/**
 * main - Everything works here
 *
 * Return: must always come back on 0
 **/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
